package diploma.math;

import java.text.DecimalFormat;
import java.text.DecimalFormatSymbols;
import java.util.Locale;

public class DoubleUtils {

//    public static double format(double x, String pattern) {
//        return Double.parseDouble(new DecimalFormat(pattern, DecimalFormatSymbols.getInstance(Locale.US)).format(x));
//    }
}
